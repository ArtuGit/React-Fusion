import { FC } from 'react';
import { Box, Container } from '@mui/material';

export const UploadPage: FC = () => {
  return (
    <Container maxWidth="lg">
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        Upload Page (work in progress)
      </Box>
    </Container>
  );
};

export default UploadPage;
